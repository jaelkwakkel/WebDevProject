namespace SetupTest
{
    public class Tests
    {
        [SetUp]
        public void Setup()
        {

        }

        [Test]
        public void Test1()
        {
            //SetupContext context;
            //ILogger<HomeController> logger;
            //UserManager<SetupUser> userManager;

            //// Arrange
            //var mockRepo = new Mock<SetupContext>();
            //mockRepo.Setup(repo => repo.GetAll()).Returns(GetTestEmployees());
            //var controller = new HomeController(context, logger, userManager);

            //// Act
            //var result = controller.Index();

            //// Assert
            //Assert.That(result, Is.TypeOf<ViewResult>());
            //var model = Assert.IsAssignableFrom<List<Employee>>(
            //    viewResult.ViewData.Model);
            //Assert.Equal(2, model.Count());
        }
    }
}